// This files contains different macros that can be used for different purposes.
// Having a set of reusable helps make development easier and makes the code more readable (IMO).

#pragma once

#define cursorVertical enableCursor(0, 15) // Makes the cursor vertical
#define cursorHorizontal enableCursor(14, 15) // Makes the cursor horizontal
